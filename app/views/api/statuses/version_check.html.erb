function update_dailog(title, text, url){
	if (Ti.App.version < title){	
			var alert_dialog = Titanium.UI.createAlertDialog({
				title : title,
				message : text,
				buttonNames : ['取消', '下载'],
				cancel : 0
			});
			alert_dialog.addEventListener("click", function(e) {
				if (e.index == 1){
					Ti.Platform.openURL(url)
					logEvent('update_from_alert1');
				}
			})
			alert_dialog.show()
	}
	else{
		show_alert("提示", "已经是最新版本");
	}
}


if (Ti.App.is_android){		//安卓
	if (Ti.App.id == "com.mamashai.babycalendar"){
		update_dailog("3.0.2", "有新版本可下载", "market://details?id=com.mamashai.babycalendar")
	}
	else if (Ti.App.id == "com.mamashai.yunfree"){
		update_dailog("3.1.2", "有新版本可下载", "market://details?id=com.mamashai.yunfree")
	}
	else if (Ti.App.id == "com.mamashai.yufree"){
		update_dailog("4.5.2", "有新版本可下载", "market://details?id=com.mamashai.yufree")
	}
}
else{			//苹果
	if (Ti.App.id == "com.mamashai.babycalendar"){
		update_dailog("3.0.1", "有新版本可下载", "http://itunes.apple.com/cn/app/bao-bao-ri-li/id499828643?ls=1&mt=8")
	}
	else if (Ti.App.id == "com.mamashai.yunfree"){
		update_dailog("3.1.1", "有新版本可下载", "https://itunes.apple.com/cn/app/yun-qi-quan-cheng-jian-ce/id479480717?l=zh&ls=1&mt=8")
	}
	else if (Ti.App.id == "com.mamashai.yufree"){
		update_dailog("4.5.1", "有新版本可下载", "https://itunes.apple.com/us/app/0-3sui-bao-bao-cheng-zhang/id485234591?l=zh&ls=1&mt=8")
	}
}
