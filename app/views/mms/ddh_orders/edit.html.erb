<h1>修改</h1>

<%= form_tag :action => 'update', :id => @ddh_order, :ddh_id=>params[:ddh_id], :page=>params[:page], :order=>params[:order]  do %>
  <%= render :partial => 'form' %>
  <%= submit_tag '保存' %> ｜ <%= link_to '返回', :action => 'list' %>
<% end %>

